<h2>List of ratings</h2>
<p>Number of ratings: <%= Rating.count%> </p>
<ul name="ratings">
  <% @ratings.each do |rating| %>
    <li id="<%= rating.id%>"> <%= rating.to_s %> <%= link_to rating.user.username , rating.user %></li>
  <% end %>
</ul>
<p>
   <%= link_to "Make a new ratings", new_rating_path%>